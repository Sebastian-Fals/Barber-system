# === BASE DE DATOS (Neon PostgreSQL) ===
# Formato: postgresql://usuario:password@host/nombre_db
DATABASE_URL="postgresql://neondb_owner:password@ep-cool-db.us-east-2.aws.neon.tech/neondb?sslmode=require"

# === WHATSAPP API ===
WHATSAPP_API_TOKEN="EAAG..."
WHATSAPP_VERIFY_TOKEN="tu_token_verificacion"

# === GOOGLE INTEGRATION ===
# Ruta relativa o absoluta al archivo JSON de credenciales (legacy, prefer GOOGLE_APPLICATION_CREDENTIALS_JSON)
GOOGLE_APPLICATION_CREDENTIALS="credentials.json"
# Credenciales JSON de la service account como string (recomendado para producción)
# Pegá aquí el contenido COMPLETO del archivo JSON de la service account
GOOGLE_APPLICATION_CREDENTIALS_JSON={"type":"service_account","project_id":"...","private_key_id":"...","private_key":"-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n","client_email":"...","client_id":"...","auth_uri":"https://accounts.google.com/o/oauth2/auth","token_uri":"https://oauth2.googleapis.com/token"}
# (Opcional) API Key para Gemini Flash si se usa
GOOGLE_API_KEY="AIza..."

# === GOOGLE CALENDAR WEBHOOK ===
# URL pública (ngrok o dominio real) donde Google enviará notificaciones
WEBHOOK_PUBLIC_URL="https://tu-app.ngrok-free.app/api/v1/google-webhook"

# === APP CONFIG ===
TIMEZONE="America/Bogota"
